// ZL Worldwide GitHub Pages automation config.
// Leave ORDER_API_URL empty for the built-in static checkout handoff.
// After deploying the Cloudflare Worker, paste its /order endpoint here.
window.ZL_AUTOMATION = {
  ORDER_API_URL: "",
  MODE: "static"
};
